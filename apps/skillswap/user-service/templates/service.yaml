apiVersion: v1
kind: Service
metadata:
  name: skillswap-user-service
  labels:
    app: skillswap-user-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: skillswap-user-service
