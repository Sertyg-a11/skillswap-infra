apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skillswap-user-service
  namespace: argocd
  labels:
    app.kubernetes.io/name: skillswap-user-service
    app.kubernetes.io/component: backend
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Sertyg-a11/skillswap-infra.git
    targetRevision: main
    path: apps/skillswap/user-service
    helm:
      valueFiles:
        - ../../../environments/dev/values-user-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: skillswap
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
